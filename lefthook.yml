pre-commit:
  parallel: true
  commands:
    lint:
      run: vendor/bin/sail bun run test:lint
    types:
      run: vendor/bin/sail bun run test:types
    pint:
      run: vendor/bin/sail bin pint --dirty --test

commit-msg:
  commands:
    commitlint:
      run: vendor/bin/sail bun run commitlint --edit {1}
