# Dependencies
/node_modules
/vendor
/auth.json
/.pnpm-store/

# Environment and secrets
.env
.env.backup
.env.production
/storage/*.key

# Build and compiled assets
/public/build
/public/hot
/public/storage
/public/fonts-manifest.dev.json
/bootstrap/ssr/

# Wayfinder (generated helpers; the Vite plugin regenerates them on dev/build)
/resources/js/actions
/resources/js/routes
/resources/js/wayfinder

# Laravel: caches and tooling
/storage/pail
/.phpunit.cache
.phpunit.result.cache
_ide_helper.php
.phpactor.json

# Static analysis / refactoring (local overrides and caches)
/phpstan.neon

# Testing (browser/Dusk artifacts)
/tests/Browser/Screenshots

# Playwright MCP (local screenshots and snapshots)
/.playwright-mcp

# Homestead
Homestead.json
Homestead.yaml

# Editors and IDEs
/.codex
/.cursor/
/.idea
/.nova
/.vscode
/.zed

# Operating system
.DS_Store
Thumbs.db

# Local coverage verification (POC artefacts, never committed)
/phpcov.phar
/coverage

# Logs
*.log
/docs/superpowers/

# Laravel Boost auto-generated guidelines (redirected here; distilled into AGENTS.md/docs by the docs skill)
/.ai/boost-guidelines.md
