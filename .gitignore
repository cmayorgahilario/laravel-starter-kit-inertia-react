# ── Environment variables ──
.env
.env.backup
.env.production
.env.*
!.env.example

# ── Dependencies ──
/node_modules
node_modules/
/vendor
vendor/
/auth.json

# ── Build output & compiled assets ──
/public/build
/public/hot
/public/storage
.next/
dist/
build/
target/

# ── Logs & caches ──
*.log
/.phpunit.cache
.phpunit.result.cache
/storage/pail
.cache/
tmp/
coverage/

# ── VitePress ──
docs/.vitepress/dist/
docs/.vitepress/cache/

# ── Storage & secrets ──
/storage/*.key

# ── Laravel / PHP tooling ──
.phpactor.json
_ide_helper.php
Homestead.json
Homestead.yaml

# ── Python ──
__pycache__/
*.pyc
.venv/
venv/

# ── IDE & editor configs ──
/.cursor/
/.idea
.idea/
/.nova
/.vscode
.vscode/
/.zed
*.code-workspace
*.swp
*.swo
*~

# ── OS-generated files ──
.DS_Store
Thumbs.db

# ── GSD baseline (auto-generated) ──
.gsd
.gsd-id
.bg-shell/

# ── MCP config (local-only, contains personal paths) ──
.mcp.json
!.mcp.json.example
